export interface UrlViewObjectInterface {
  id: string;
  code: string;
  target_url: string;
  short_url: string;
}

export interface UrlInterface {
  id: string;
  code: string;
  targetUrl: string;
  shortUrl: string;
}
